# API base URL — points to the deployed Fly.io backend
EXPO_PUBLIC_API_BASE_URL=https://bubble-workout-api.fly.dev

# Internal engine authentication key — must match ENGINE_KEY set on the server
# Obtain from the team; do NOT commit the real value
EXPO_PUBLIC_ENGINE_KEY=
